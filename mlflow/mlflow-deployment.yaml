apiVersion: v1
kind: Namespace
metadata:
  name: mlflow
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mlflow-tracking
  namespace: mlflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mlflow
  template:
    metadata:
      labels:
        app: mlflow
    spec:
      containers:
      - name: mlflow
        image: ghcr.io/mlflow/mlflow:v2.11.3 # Standard community MLflow image
        command: ["mlflow", "server", "--host", "0.0.0.0", "--default-artifact-root", "s3://mlflow-bucket/"]
        env:
        - name: AWS_ACCESS_KEY_ID
          value: "mock_aws_access_key"
        - name: AWS_SECRET_ACCESS_KEY
          value: "mock_aws_secret_key"
        - name: MLFLOW_S3_ENDPOINT_URL
          value: "http://s3-service.aws-mock.svc.cluster.local:9000"
        - name: AWS_DEFAULT_REGION
          value: "us-east-1"
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: mlflow-service
  namespace: mlflow
spec:
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    app: mlflow